export const regex = {
  email: /^[a-zA-Z0-9-.]+@[a-zA-Z0-9]+\.[A-Za-z]+$/,
};

export const MIN_PASSWORD = 8;

export const path = {
  SIGN_IN: '/sign-in',
  SIGN_UP: '/sign-up',
  SIGN_OUT: '/sign-out',
  GALLERY: '/gallery',
  PROFILE: '/profile',
};
